function on_gui_changed(~, ~, ~)
% on_gui_changed(obj, propName, value)
% Hook for subclasses to respond to GUI-driven property changes.
% Override in subclasses, e.g. to update dependent widget formatting.
end
